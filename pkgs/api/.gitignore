node_modules/
.DS_Store
THUMBS_DB
tsconfig.tsbuildinfo
dist/

# (Generated artifacts under src/**/__generated__ ARE committed so
# downstream builds don't need to install + run openapi-typescript-codegen
# at build time. Regenerate via `pnpm tradingapi:generate:openapi` and
# commit the diff.)
